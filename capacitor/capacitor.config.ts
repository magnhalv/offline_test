import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.capacitordownloadtest',
  appName: 'Norway Map',
  webDir: 'dist',
};

export default config;
